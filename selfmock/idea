dmocks -- autogenerated mock objects -- isn't working yet.
I can do a CppMock sort of thing: you create an object like this:

class Actual : IActual
{
    type method1(types....) {}
    // ...
}

class MockActual : Mock, IActual
{
    type method1(types....) 
    {
        return selfmock.method!("method1", type, types)();
    }
    // ...
}

Then you do something like:

unittest
{
    auto dependency = new MockActual;
    auto mocks = new Mocker;
    mocks.register(dependency);
    auto tested = new Tested(dependency);
    // ...
}

It costs one line in your test, plus a maintenance hassle.

I could probably have an interface like:
class MockActual : IActual
{
    mixin(method!(&IActual.opIndex));
}


And I need it to work with Tango.
